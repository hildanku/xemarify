# Untuk build-time frontend (dibaca saat docker build).
# Harus URL API yang bisa diakses browser user akhir.
VITE_API_BASE_URL=http://localhost:8089

# Runtime untuk SvelteKit adapter-node
ORIGIN=http://localhost:3000
HOST=0.0.0.0
PORT=3000
